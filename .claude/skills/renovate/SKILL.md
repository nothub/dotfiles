---
name: renovate
description: Configure Renovate for dependency update automation in any project
---

Every project should have a `renovate.json` for automated dependency updates.

See `references/renovate.json` for the standard config file. Use it as-is unless the project has specific needs.
