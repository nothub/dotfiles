---
description: Audit and maintain this `.claude/` directory — skill descriptions, personas, commands, references, and README.
---

Invoke the `claude-janitor` skill for the `${HOME}/.dotfiles/.claude/` dir.
