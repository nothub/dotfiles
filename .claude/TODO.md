# TODOs

## Command Order

Swap `/build` and `/plan` in the `Orchestration commands` section in `.claude/README.md`.  
`/plan` should be done before `/build` we want to reflect the real-world usage in the order.  
Also apply this order for the `Commands` section above.

## Go Skill Extraction

Discuss Pro/Contra of extracting a Go skill from `go-cli-project`, and `go-web-project`.

## Readme

Is the `.claude/README.md` up to date?

## Claude Dir Maintenance

Is all infos in `.claude/skills/claude-dir-maintenance/` in sync?
Seems outdated. Still has info about handoffs. Was the skill ever finished? 
