#!/usr/bin/env bash

set -o errexit

if test "${1:-}" = --list-commands; then
    printf '\t<path>...\n'
    exit 0
fi

shfmt \
    --write \
    --simplify \
    --indent 4 \
    --binary-next-line \
    --case-indent \
    --space-redirects \
    "${@}"
